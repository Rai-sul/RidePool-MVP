diff --git a/node_modules/@react-navigation/core/lib/module/NavigationBuilderContext.js b/node_modules/@react-navigation/core/lib/module/NavigationBuilderContext.js
index 282706b..7089760 100644
--- a/node_modules/@react-navigation/core/lib/module/NavigationBuilderContext.js
+++ b/node_modules/@react-navigation/core/lib/module/NavigationBuilderContext.js
@@ -1,17 +1,15 @@
 "use strict";
 
 import * as React from 'react';
+const NOOP = () => {};
 /**
  * Context which holds the required helpers needed to build nested navigators.
+ * Patched: no-op defaults instead of throwing during transient context loss.
  */
 export const NavigationBuilderContext = /*#__PURE__*/React.createContext({
   onDispatchAction: () => undefined,
   onOptionsChange: () => undefined,
-  scheduleUpdate: () => {
-    throw new Error("Couldn't find a context for scheduling updates.");
-  },
-  flushUpdates: () => {
-    throw new Error("Couldn't find a context for flushing updates.");
-  }
+  scheduleUpdate: NOOP,
+  flushUpdates: NOOP,
 });
 //# sourceMappingURL=NavigationBuilderContext.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/NavigationStateContext.js b/node_modules/@react-navigation/core/lib/module/NavigationStateContext.js
index 4221b52..67e5839 100644
--- a/node_modules/@react-navigation/core/lib/module/NavigationStateContext.js
+++ b/node_modules/@react-navigation/core/lib/module/NavigationStateContext.js
@@ -2,22 +2,18 @@
 
 import * as React from 'react';
 const MISSING_CONTEXT_ERROR = "Couldn't find a navigation context. Have you wrapped your app with 'NavigationContainer'? See https://reactnavigation.org/docs/getting-started for setup instructions.";
+
+// Patched: return safe no-op defaults instead of throwing, so transient
+// context loss during React 19 concurrent re-renders doesn't crash the app.
+const NOOP = () => {};
+const NOOP_UNSUB = () => NOOP;
 export const NavigationStateContext = /*#__PURE__*/React.createContext({
   isDefault: true,
-  get getKey() {
-    throw new Error(MISSING_CONTEXT_ERROR);
-  },
-  get setKey() {
-    throw new Error(MISSING_CONTEXT_ERROR);
-  },
-  get getState() {
-    throw new Error(MISSING_CONTEXT_ERROR);
-  },
-  get setState() {
-    throw new Error(MISSING_CONTEXT_ERROR);
-  },
-  get getIsInitial() {
-    throw new Error(MISSING_CONTEXT_ERROR);
-  }
+  getKey: NOOP,
+  setKey: NOOP,
+  getState: () => undefined,
+  setState: NOOP,
+  getIsInitial: () => true,
+  addOptionsGetter: NOOP_UNSUB,
 });
 //# sourceMappingURL=NavigationStateContext.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigation.js b/node_modules/@react-navigation/core/lib/module/useNavigation.js
index ee7916d..2e9d08d 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigation.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigation.js
@@ -3,6 +3,23 @@
 import * as React from 'react';
 import { NavigationContainerRefContext } from "./NavigationContainerRefContext.js";
 import { NavigationContext } from "./NavigationContext.js";
+
+// Patched: Create a safe no-op navigation object for transient context loss
+const SAFE_NAVIGATION = {
+  navigate: () => {},
+  goBack: () => {},
+  reset: () => {},
+  setParams: () => {},
+  dispatch: () => {},
+  isFocused: () => true,
+  canGoBack: () => false,
+  getId: () => undefined,
+  getParent: () => undefined,
+  getState: () => undefined,
+  addListener: () => () => {},
+  removeListener: () => {},
+};
+
 /**
  * Hook to access the navigation prop of the parent screen anywhere.
  *
@@ -12,7 +29,10 @@ export function useNavigation() {
   const root = React.useContext(NavigationContainerRefContext);
   const navigation = React.useContext(NavigationContext);
   if (navigation === undefined && root === undefined) {
-    throw new Error("Couldn't find a navigation object. Is your component inside NavigationContainer?");
+    // Patched: Return safe no-op navigation during transient context loss
+    // instead of throwing, to handle React 19 concurrent re-renders gracefully.
+    console.warn('[react-navigation] Navigation context temporarily unavailable');
+    return SAFE_NAVIGATION;
   }
 
   // FIXME: Figure out a better way to do this
