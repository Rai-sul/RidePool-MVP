import * as React from "react";
import { View } from "react-native";

// React Native toast is handled differently - this is a placeholder
// In the actual implementation, toast notifications are shown using
// a toast library compatible with React Native (already imported in App.native.tsx)

const Toaster = () => {
  return <View />;
};

export { Toaster };
